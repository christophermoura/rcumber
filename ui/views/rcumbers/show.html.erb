<%= link_to 'see all story tests', '/rcumber' %>
| <%= link_to 'run this story test', rcumber_run_path(@rcumber.uid)%>
<%= rcumber_flashes %>
<h1><%= @rcumber.name %></h1>
<% form_for @rcumber, :url => rcumber_path(@rcumber.uid) do |f| %>
	<%= f.text_area :raw_content, :cols => 80 %>
	<br/>
	<br/>
		<%= submit_tag 'Save' %>
<% end %>
<% unless @rcumber.last_results.nil? %>
	<h1>Test Results</h1>
	<ul>
		<% @rcumber.last_results.each do |line| %>
			<%= content_tag :li, line %>
		<% end %>
	</ul>
<% end %>